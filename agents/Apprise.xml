<?xml version="1.0" encoding="utf-8"?>
  <Agent>
    <Name>Apprise Notification Agent</Name>
    <Variables>
      <Variable Help="One or more Apprise URLs or tagged entries. Separate multiple entries with semicolons. Example: tag1=tgram://BOT_TOKEN/CHAT_ID; tag2=discord://WEBHOOK_ID/WEBHOOK_TOKEN[br][br]See Apprise [a href='https://appriseit.com/services/' target='_blank'][u]Supported Services[/u][/a] for available targets and the [a href='https://appriseit.com/url-builder/' target='_blank'][u]URL Builder[/u][/a] for help creating URLs." Desc="Apprise URL(s)" Default="">APPRISE_URLS</Variable>
      <Variable Help="Optional filter for tagged Apprise entries. Use 'none' to send to every configured entry. Example: tag1 or tag1,tag2" Desc="Tag Filter" Default="none">TAGS</Variable>
      <Variable Help="Specify the fields which are included in the title of the notification." Desc="Notification Title" Default="$SUBJECT">TITLE</Variable>
      <Variable Help="Specify the fields which are included in the message body of the notification." Desc="Notification Message" Default="$DESCRIPTION">MESSAGE</Variable>
    </Variables>
    <Script>
      <![CDATA[
#!/bin/bash
############
{0}
############

if [[ -n "${APPRISE_RELAY_TITLE:-}" ]]; then
  TITLE="$APPRISE_RELAY_TITLE"
fi
if [[ -n "${APPRISE_RELAY_MESSAGE:-}" ]]; then
  MESSAGE="$APPRISE_RELAY_MESSAGE"
fi
if [[ -n "${APPRISE_RELAY_TAGS:-}" ]]; then
  TAGS="$APPRISE_RELAY_TAGS"
fi

SCRIPTNAME=$(basename "$0")
LOG="/var/log/notify_${SCRIPTNAME%.*}"

if ! command -v apprise >/dev/null 2>&1; then
  echo "apprise command not found" >>"$LOG"
  logger -t "$SCRIPTNAME" -- "Failed sending notification (apprise command not found)"
  exit 1
fi

TITLE=$(echo -e "$TITLE")
MESSAGE=$(echo -e "$MESSAGE")

case "$IMPORTANCE" in
  normal)
    NOTIFY_TYPE="info"
    ;;
  warning)
    NOTIFY_TYPE="warning"
    ;;
  alert)
    NOTIFY_TYPE="failure"
    ;;
  *)
    NOTIFY_TYPE="info"
    ;;
esac

CONFIG=$(mktemp /tmp/apprise-agent.XXXXXX.cfg)
trap 'rm -f "$CONFIG"' EXIT
printf '%s\n' "$APPRISE_URLS" > "$CONFIG"

if [[ -z "$(tr -d '[:space:];,' < "$CONFIG")" ]]; then
  echo "No Apprise URLs configured" >>"$LOG"
  logger -t "$SCRIPTNAME" -- "Failed sending notification (no Apprise URLs configured)"
  exit 1
fi

args=(-c "$CONFIG" -t "$TITLE" -b "$MESSAGE" -n "$NOTIFY_TYPE")
if [[ -n "$TAGS" && "$TAGS" != "none" ]]; then
  tags="${TAGS//,/ }"
  for tag in $tags; do
    args+=(-g "$tag")
  done
fi

output=$(apprise "${args[@]}" 2>&1)
status=$?
if [[ "$status" -ne 0 ]]; then
  echo "$output" >>"$LOG"
  logger -t "$SCRIPTNAME" -- "Failed sending notification ($output)"
fi
exit "$status"
      ]]>
    </Script>
  </Agent>
